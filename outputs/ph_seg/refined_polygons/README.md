Refined PHI polygons (EPSG:27700, original attributes kept, `pid` = row index in the input geojson, `method` column).
`<site>_refined_vote.geojson` is the recommended layer (majority of grabcut2 / sam2iter / sam2tile, overlaps resolved, simplified 0.6 m).
Parts of polygons outside the imagery footprint are unchanged. See outputs/ph_seg/report/report.html.
